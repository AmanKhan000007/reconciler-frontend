# ── Reconciler Frontend Environment ──────────────────────────────────────────
#
# Copy this file to .env and fill in your values.
# For production (Vercel / Netlify / Render), set these as environment variables.
#
# The deployed Render backend URL (from your render.yaml):
#VITE_API_URL=https://reconciler-backend.onrender.com
#
# For local dev (backend running on localhost:5000):
VITE_API_URL=http://localhost:5000
